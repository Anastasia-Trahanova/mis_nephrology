"""
Слой доступа к данным.

Папка repositories содержит маленькие функции, которые выполняют конкретные
SQL-операции. В этих файлах нет бизнес-логики приёма, медицинских расчётов,
FastAPI request/session и HTML.

Правило слоя:
- repository получает открытый cursor;
- выполняет один понятный SELECT/INSERT;
- возвращает id, строку или ничего;
- commit/rollback делает сервис выше.
"""
